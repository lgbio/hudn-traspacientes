The app is running but there are some issues to correct/improve:
- When editing a previous existing record, the date field "Fecha" is lost.

- When adding a new record, the default value of "Fecha" must be the current day.

- The widget for selecting a time (HH:MM am/pm) remains visible after selecting all time elements: hour, minutes, am/pm. Maybe it must have an accept ("Aceptar") button or something like this, or when selecting am/pm then it sets the time an dissapear. Currently, the user has to click outside the widget to close it.

